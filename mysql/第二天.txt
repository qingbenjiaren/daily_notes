哪些情况不需要创建索引
	表记录太少，索引是要有存储的开销
	频繁更新，索引要维护
	查询字段使用频率不高


当查询条件查询出来的结果大于全表记录的三分之一时，索引失效


等值非主键连接，两边都需要建立索引，不然就会全表扫描

sql优化：id>0 order by id


当出现ALL时，说明数据在存储引擎层没有做任何过滤，全部去SQLLAyer层进行过滤
这样的IO开销很大

索引条件下推


组合索引必须连续使用
问题：
索引下推
覆盖索引


乐观锁和悲观锁的概念

乐观锁：默认无锁：需要的时候再加
悲观锁：默认有锁