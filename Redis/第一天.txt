5种类型：
	字符串
	哈希
	列表
	集合
	有序集合
	
	每种类型可能有两种或更多的内部编码实现
	
Redis内存分配
	数据
	进程
	缓冲内存
	内存碎片
	
Redis数据结构

	简单动态字符串SDS
	
	使用：
		所有的key
		数据里的字符串
		
	**链表**
		链表和列表的区别
		存的是指针
	
	字典
		key-value
		
	**跳跃表（一种特殊的链表）**
		以空间换时间
		抛硬币（随机），随机有点东西，搞搞，现在的理解是，需要随机一个层数出来，还牵扯到概率论
		java与数据结构
		
		
	整数集合
		整数集合（intset）是集合（set）的底层实现之一
		
	压缩列表
		为节省内存而开发
	
	对象
		
	缓存淘汰策略
		LRU（最近最少使用）LinkedHashMap
		把经常使用的数据放到redis里，Redis自带有线程安全的LRU算法（单线程），redis会自动把最近最少使用的数据清理掉，如果下次再访问又重新拿到redis就行了
		
	Redis事务(把命令放入队列里)
		multi
				。。。。
				。。。。
		exec/discard
		
		
		watch
		
		事务内，
			语法错误之后的都失败
			运行期的错误不影响其他命令的成功执行
			
		乐观锁：基于CAS（Compare And Swap）思想，是不具有互斥性，不会产生锁等待而消耗资源
		
		
			秒杀20个，不是最先来的20个就秒到，而是随机的，一波人只有一人成功，一个成功之后，又有一波人中一人成功，依此类推
		
		
		
		